"""Фаза 61: у группы больше нет текстового поля «куратор».

Источник права — назначение с датой (`accounts.CuratorAssignment`,
фаза 60). Поле с именем осталось от времени, когда куратора в системе
не было: по строке нельзя ни войти, ни проверить право, а два источника
одного и того же расходятся в первый же месяц (инвариант №2).

**Перед `migrate` на боевой машине назначьте кураторов всем группам.**
Имена из поля миграция никуда не переносит и перенести не может:
сопоставить «Асель» с учётной записью умеет только человек. Откат
(`migrate students 0015`) возвращает колонку пустой — записанные имена
после удаления не восстанавливаются ничем, кроме резервной копии.
"""

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ("students", "0015_phase59"),
    ]

    operations = [
        migrations.RemoveField(
            model_name="studygroup",
            name="curator",
        ),
    ]
