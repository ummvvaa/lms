# Реестр дефектов — аудит фаз 1–6

Дата аудита: 2026-08-22. Фаза 7.
Контур: `docker compose up`, фронт `http://localhost:5173`, тот же фронт через nginx `http://localhost:8080`.
База наполнена сгенерированными данными разработки (87 учеников, 10 вузов, 6 выпускников);
команда наполнения удалена из кода в фазе 22.

Чем проверялось:

* Playwright, каталог `e2e/` — 96 проверок «роль × экран», на каждом экране нажата каждая
  видимая кнопка с проверкой сети, консоли и логов бэкенда (`e2e/tests/audit.spec.ts`);
* сценарные проверки сохранения и перезагрузки (`e2e/tests/scenarios.spec.ts`);
* прогон API под шестью ролями напрямую (`e2e/api_probe.py`).

Ничего из найденного здесь не чинилось — это задача фазы 8.

## Как читать таблицу

Серьёзность: **блокер** — работать нельзя; **важно** — работать можно, но неправильно
или вслепую; **косметика** — мешает, но не врёт.

---

## Блокеры

| № | Экран | Роль | Что сделал | Что ожидалось | Что произошло |
| --- | --- | --- | --- | --- | --- |
| B1 | любой | все шесть | Нажал любую кнопку, меняющую данные: «Сохранить» в таблице, «Сохранить версию» в эссе, «Что даст +0.5 IELTS», «Передать выпускнику», «Применить» в импорте, даже «Выйти» | Запрос уходит, ответ 2xx, изменение видно после перезагрузки | Ответ `403 CSRF Failed: Origin checking failed`. **Ни одна запись из браузера не проходит.** Причина: `CSRF_TRUSTED_ORIGINS` читается только в `config/settings/prod.py`, а значение из `deploy/.env` в dev-настройки не попадает; nginx к тому же передаёт `Host $host` без порта. Вход работает лишь потому, что DRF не проверяет CSRF у неаутентифицированного запроса — после входа отваливается всё |
| B2 | `/table` | все директора | Открыл таблицу при 87 учениках в базе | Видны все 87 | Видны первые 50. Фронт просит `?page_size=250`, но в `REST_FRAMEWORK` не задан `page_size_query_param`, поэтому параметр игнорируется. Пагинации в интерфейсе нет, счётчик «50 учеников» врёт, 37 человек не видно и об этом ничего не сказано |
| B3 | `/table` | все директора | Ввёл в числовую ячейку («IELTS текущий») текст `не число` и нажал «Сохранить» | Строка отклоняется, остальные сохраняются, человек видит причину | `HTTP 500`, страница Django с трассировкой. `core.audit.normalize` глотает `ValidationError` и возвращает исходную строку, падение уезжает в `instance.save()`. Воспроизводится и напрямую: `POST /api/batch/save/` с `{"field":"ielts_current","value":"не число"}` |
| B4 | `/assistant` | все директора | Попробовал нажать «Массовое действие», «Дайджест на сегодня», «Объясни этот список», «Разобрать мок», «План подготовки», «Разобрать вуз», «Проверить баланс списка», «Разобрать активность», «Gap → задачи», «Распознать сертификат» | Действие выполняется | Это не кнопки. `Assistant.tsx` рисует их как `<div className="card">` без обработчика. Из двенадцати объявленных в `suggestions/commands.py` действий эндпойнт есть у двух (`commands/paste/`, `commands/upload/`), к интерфейсу подключено одно — «Вставить как есть». Ещё два эндпойнта (`commands/explain-match/`, `commands/essay-questions/`) не вызываются ниоткуда |

| B5 | `/table` | все директора | Открыл таблицу и посмотрел на ячейки | Видны текущие значения домена | Все ячейки пустые всегда: `StudentListSerializer` отдавал только имя, класс и группу — профилей в ответе не было вовсе. Директор правил вслепую, а «Сохранить» уходило с `expected: ""`, из-за чего каждое изменение непустого поля превращалось в конфликт и молча пропадало. Найден в фазе 8 при проверке сохранения браузером — обход кнопками его не видел, потому что в ячейки никто не печатал |

## Важно

| № | Экран | Роль | Что сделал | Что ожидалось | Что произошло |
| --- | --- | --- | --- | --- | --- |
| I1 | `/students/:id` | все директора | Правил поле своего домена на карточке | Как в таблице: сервер сверяет прежнее значение и отдаёт конфликт, если кто-то успел поправить | `StudentCard.tsx` не кладёт в изменение `expected`. Защита от тихой перезаписи, ради которой она делалась (решение от 2026-08-21), на карточке не работает — а карточка и есть основной экран правки одного ученика |
| I2 | `/digest` | все директора | Увидел «Предложение #12 · paste_as_is», нажал на строку | Открывается предпросмотр предложения | Строка не кликабельна. Экрана списка предложений нет вообще: хук `useSuggestions` объявлен в `api/hooks.ts` и не используется ни разу. Предложение можно открыть только сразу после разбора, в той же вкладке. Закрыл вкладку — предложение потеряно |
| I3 | `/groups`, `/risks`, `/deadlines`, `/top30`, `/tracks`, `/competitions`, `/overview` | все директора | Нажал пункт навигации своей роли | Экран раздела | Все семь маршрутов в `App.tsx` отдают тот же `<Dashboard />`, что и `/dashboard`. Семь пунктов меню ведут в одно и то же место; ни одного собственного экрана у них нет |
| I4 | `/roadmap`, `/universities` | все пять директоров | Открыл адрес напрямую (в навигации его нет, но маршрут открыт) | Отказ или переход на свой дашборд | Экран рисуется наполовину, в консоли `404` от `/api/tasks/my/` и `/api/match/my-universities/`: у сотрудника нет карточки ученика. Разграничения маршрутов по ролям в роутере нет вовсе |
| I5 | `/dashboard` | `director_exam` | Нажал столбец гистограммы «12 IELTS < 6.0» | Список этих учеников или фильтр таблицы | Ничего. Столбцы размечены как `<button>`, обработчика нет. Три такие кнопки на каждом заходе |
| I6 | `/table`, `/students/:id` | все директора | Наменял в черновике лишнего и передумал | Кнопка «Отменить» возвращает прежние значения | Кнопки отмены нет ни там, ни там. Сбросить черновик можно только перезагрузкой страницы, и таблица при этом переспрашивает через `beforeunload` |
| I7 | `/import` | все директора | Искал импорт в меню | Пункт в навигации | Пункта нет ни у одной роли. Экран открывается только кнопкой «Импорт из файла» внутри таблицы; по прямой ссылке работает |
| I8 | `/login` | все | Открыл вход | Работающие способы входа | Первая и самая заметная кнопка «Войти через Microsoft» всегда `disabled`: `ENTRA_CLIENT_ID` в контуре пуст. Человек видит выключенную главную кнопку и не понимает, сломано это или так задумано |
| I9 | админка, shell | — | Поменял поле профиля через `/admin/` | В журнале виден автор правки | `core/signals.py` пишет `actor=None` — правка в журнале анонимна. Инвариант №9 формально соблюдён (запись есть), фактически «кто» потеряно. Долг известен и записан в `STATE.md`, но не закрыт |
| I10 | `/assistant` | `director_sport`, `director_talent`, `director_admission` | Искал разбор фото сертификата, скриншота, названия вуза | Заявлено в фазе 5 как сделанное | Обработчиков нет: ни `parse_certificate`, ни `parse_activity`, ни `parse_university`. Записано в `STATE.md` как долг, но в отчётах фаз числится сделанным |

## Косметика

| № | Экран | Роль | Что сделал | Что ожидалось | Что произошло |
| --- | --- | --- | --- | --- | --- |
| C1 | все экраны | все | Посмотрел на заголовок | Один заголовок | `ScreenHead` в `components/ui.tsx` печатает `title` дважды — в надзаголовке и в `<h1>`: «Роадмап Роадмап», «Эссе Эссе», «Мои вузы Мои вузы» |
| C2 | все экраны | `student` | Нажал «Позже» в баннере привязки почты | Баннер больше не мешает | Возвращается при каждом переходе: состояние живёт в компоненте, а компонент перемонтируется. На каждом экране ученика первые две кнопки — чужие |
| C3 | баннер привязки | `student` | Нажал «Привязать» с пустым полем | Внятная подсказка | Только нативный пузырёк браузера; своего сообщения нет |
| C4 | `/dashboard` | `student` с пустым профилем | Открыл кабинет | Готовность 0% и пять блоков | Готовность 0%, но в разбивке только два блока из пяти — «Поступление» и «Портфолио». Экзамены, дисциплина и спорт не показаны вовсе, хотя вес у них есть |

---

## Что заявлено сделанным в фазах 1–6, но не работает

1. **Фаза 3, «батч-сохранение с защитой от перезаписи».** Работает в таблице, но не на карточке
   ученика (I1). А из браузера не работает нигде из-за B1.
2. **Фаза 3, «табличный режим на 250 учеников».** Отдаётся ровно 50 (B2). Критерий
   «меньше секунды на 250» проверялся, видимо, на бэкенде — до интерфейса эти строки не доходят.
3. **Фаза 5, «именованные кнопки, а не чат».** Кнопок двенадцать, работает одна (B4).
4. **Фаза 5, «инструменты модели».** `explain_match` и `essay_questions` реализованы на бэке
   и не подключены к интерфейсу — снаружи их нет.
5. **Фаза 5, «предпросмотр и применение предложений».** Экран есть, но добраться до него
   можно только сразу после разбора: списка предложений нет (I2).
6. **Фаза 6, «дайджест открывается при входе».** Дайджест — отдельный пункт меню, при входе
   не открывается; строки в нём не ведут никуда (I2).
7. **Разделы навигации директоров** (`Группы`, `Риски`, `Дедлайны`, `TOP-30`, `Треки`,
   `Соревнования`, `Сводный вид`) в отчётах фаз упомянуты как разделы. Экранов нет (I3).
8. **Разбор скриншотов, фото сертификатов, `parse_university`** — кнопки есть, обработчиков нет
   (I10). Это уже записано в `STATE.md` как долг; здесь фиксируется, что за фазу ничего не изменилось.

## Что проверено и работает

Чтобы реестр не выглядел приговором всему, отдельно: права по доменам держатся.
Прогон `e2e/api_probe.py` под шестью ролями прошёл чисто по всем пунктам, кроме B3.

* Ученику не отдаются внутренние ярлыки ни в `/api/students/me/`, ни в `/api/meta/domains/`,
  ни в подборе вузов, ни в задачах (инвариант №7).
* Ученик получает 403 на дашборды директоров и на сводный вид, 404 на чужую карточку
  и чужую историю, 403 на любую запись — включая собственный профиль.
* Директор не может записать в чужой домен ни через `PATCH /api/profiles/<домен>/`,
  ни через батч: строка возвращается в `rejected` с причиной «Поле чужого домена».
* Чужой домен при этом читается — как и задумано.
* Батч с устаревшим `expected` отдаёт конфликт, а не перезаписывает.
* Пагинация, сортировка и фильтры API отвечают корректно и внятно ругаются на мусор
  (`grade=abc` → 400 «Введите число»), кроме игнорируемого `page_size` (B2).
* Экраны на пустых данных не выглядят сломанными: везде есть человеческая заглушка,
  исключений в консоли нет.
* Дашборды всех пяти доменов открываются любому директору без ошибок.

## Статусы починки

Заполнено в фазе 8. Блокеров не осталось; единственный незакрытый пункт — I10,
отложен с причиной.

| № | Статус | Комментарий |
| --- | --- | --- |
| B1 | исправлено | `CSRF_TRUSTED_ORIGINS` читается в `config/settings/base.py` из окружения, умолчание покрывает 5173 и 8080; `deploy/.env.example` обновлён. Тест: `e2e/tests/fixed.spec.ts` — what-if и «Выйти» отвечают 2xx |
| B2 | исправлено | `core/pagination.py`: `page_size` до 500. Таблица просит 500, счётчик показывает «N из M», при переполнении появляется постраничная навигация. Тесты: `core/tests/test_pagination.py`, `e2e/tests/scenarios.spec.ts` |
| B3 | исправлено | `core.audit.coerce` отказывает с человеческим текстом вместо падения; батч, импорт и применение предложений отклоняют строку, а не запрос. Причина показывается в интерфейсе. Тесты: `students/tests/test_batch_validation.py` |
| B4 | исправлено | Карточки стали кнопками с обработчиками. Реестр сокращён до построенного, непостроенное вынесено в `NOT_BUILT_YET` и в интерфейсе не предлагается. Добавлены «Проверить баланс списка» и «Объясни соответствие». Тесты: `suggestions/tests/test_acceptance.py::test_every_offered_command_is_actually_built`, `e2e/tests/fixed.spec.ts` |
| B5 | исправлено | `StudentListSerializer` отдаёт пять профилей; ярлыки по-прежнему режет `DomainModelSerializer`. Заодно порядок списка стал устойчивым (`ordering` с `id`), иначе строки перескакивают между страницами. Тест: `e2e/tests/scenarios.spec.ts` — правка переживает перезагрузку |
| I1 | исправлено | Карточка шлёт `expected` — как таблица. Тест: `e2e/tests/scenarios.spec.ts` проверяет и сам факт отправки `expected`, и запись в истории |
| I2 | исправлено | Экран `/suggestions` в навигации, строки дайджеста ведут на конкретное предложение. Тест: `e2e/tests/fixed.spec.ts` |
| I3 | исправлено | Разделы стали якорями секций дашборда: маршрут прокручивает к блоку и подсвечивает его. Тест: `e2e/tests/fixed.spec.ts` — `/top30` доводит до секции |
| I4 | исправлено | `Protected` в `App.tsx` уводит на свой дашборд по спискам `STUDENT_ONLY`/`STAFF_ONLY`. Тест: `e2e/tests/fixed.spec.ts` — переход без единого 404 в консоли |
| I5 | исправлено | Плитка гистограммы ведёт в таблицу с фильтром по диапазону; фильтр виден и снимается. Тест: `e2e/tests/fixed.spec.ts` |
| I6 | исправлено | «Отменить правки» в таблице и «Отменить» на карточке. Тест: `e2e/tests/fixed.spec.ts` |
| I7 | исправлено | «Импорт» — пункт навигации у всех директоров |
| I8 | исправлено | Кнопка Microsoft показывается только при настроенном Entra. В фазе 9 уходит совсем |
| I9 | исправлено | `core/actor.py`: актор кладётся в контекстную переменную запроса, сигнальный аудит читает его оттуда. Для команд и задач есть `acting_as`. Тесты: `core/tests/test_actor.py`, включая проверку, что актор не течёт за пределы блока |
| I10 | отложено | Разбор фото и скриншотов требует OCR, `parse_university` — разбора произвольной вёрстки. Ни того, ни другого в проекте нет. Кнопки убраны из реестра (`NOT_BUILT_YET`), чтобы интерфейс не обещал несуществующего |
| C1 | исправлено | `ScreenHead` печатает заголовок один раз; надзаголовок — плашка-эмодзи. Тест: `e2e/tests/fixed.spec.ts` |
| C2 | исправлено | «Позже» запоминается в `localStorage`. Тест: `e2e/tests/fixed.spec.ts` |
| C3 | исправлено | Под формой привязки появилась подсказка, чего от человека хотят |
| C4 | исправлено | `Readiness` отдаёт `skipped` — домены без данных; ученик видит все пять блоков, три из них подписаны «данных пока нет». Тесты: `core/tests/test_readiness_skipped.py`, `e2e/tests/fixed.spec.ts` |

---

# Реестр дефектов — финальная проверка глазами, фаза 16

Дата: 2026-08-23. Контур тот же, база наполнена сгенерированными данными разработки
(49 учеников, 29 вузов); команда наполнения удалена из кода в фазе 22.

Чем проверялось: `e2e/tests/shots.spec.ts` снял 31 кадр — семь ролей × их экраны,
две ширины (1440 ноутбук и 820 планшет), плюс кадр первого входа. Кадры
просмотрены глазами. Искали то, что просила фаза: пустые области без объяснения,
обрезанный и наезжающий текст, кнопки без подписи, английские слова в русском
интерфейсе, элементы за пределами экрана, нечитаемые сочетания цветов, кнопки
удаления рядом с сохранением.

Всё найденное закрыто в этой же фазе.

## Блокеры

| № | Экран | Что увидели | Причина | Как починили |
| --- | --- | --- | --- | --- |
| V1 | все экраны, ширина 820 | **Каждый экран уезжает вбок.** Кадр шире окна на 50–550 px: у директора дисциплины 1317 px при окне 820. Часть навигации и правый край карточек за краем | `.shell` — грид с колонкой `1fr`, минимальная ширина которой равна `min-content` содержимого. Лента навигации в строку не переносилась и распирала страницу; `overflow-x: auto` на ленте не помогал | `grid-template-columns: 248px minmax(0, 1fr)`, в узком режиме `minmax(0, 1fr)`; лента навигации переносится (`flex-wrap: wrap`), а не прокручивается |
| V2 | `/users`, ширина 820 | Таблица пользователей шире окна, вбок уезжает вся страница | Таблица без своего контейнера прокрутки | Таблица прокручивается внутри карточки (`.users__wrap { overflow-x: auto }`), страница стоит на месте |

## Важно

| № | Экран | Что увидели | Как починили |
| --- | --- | --- | --- |
| V3 | `/dashboard` админа, ширина 820 | Под лентой навигации полоса пустоты в пол-экрана; четыре пункта меню за правым краем | `grid-template-rows: auto 1fr` в узком режиме плюс перенос ленты — видны все одиннадцать пунктов |
| V4 | `/import` | Кнопка выбора файла подписана **«Choose File»**, рядом «No file chosen» | Нативную кнопку `input[type=file]` переопределить нельзя — сделали свой ярлык «Выбрать файл» со скрытым полем и именем файла рядом |
| V5 | `/dashboard` ученика | Панель «С чего начать» и баннер «Расскажите о себе» идут подряд и говорят одно и то же | Баннер убран: первая строка чеклиста ведёт туда же |
| V6 | `/dashboard` ученика | «Задания на сегодня»: абзац в верхнем углу и 250 px пустоты под ним | Пустое состояние с объяснением и кнопкой «Открыть каталог» |
| V7 | `/dashboard` экзаменов, `/prep` | Список платформенных пробных не ограничен: 15 одинаковых строк съедают экран. То же в истории загрузок и в «Пройденных пробных» | Показываем 8–10 строк, ждущие решения сверху, остальное по кнопке «Показать все» |
| V8 | `/dashboard` поступления | Дедлайнов нет — на экране голая строка «Дедлайнов в ближайшие 120 дней нет.», переносящаяся посреди фразы | Пустое состояние с объяснением, откуда берутся дедлайны, и кнопкой в справочник |

## Косметика

| № | Экран | Что увидели | Как починили |
| --- | --- | --- | --- |
| V9 | `/dashboard` поступления | Английские подписи **«University slots»** и **«Admission readiness»** | «Мест в списках», «Готовность к подаче» |
| V10 | `/dashboard` талантов | Трек **«Competition»** остался английским среди русских | «Конкурсы» |
| V11 | `/dashboard` экзаменов | «Мок упал — нужна **интервенция**» | «Мок упал — нужно вмешаться» |
| V12 | `/universities` | «**1 программ** в вашем списке» | Склонение числительных: `plural()` и `counted()` в `components/ui.tsx`, применены в «Моих вузах», роадмапе, таблице и пользователях |
| V13 | `/universities` | В карточке соответствия подписи рвутся на три строки: «IELTS или / TOEFL», «не хватает 4.5 / IELTS» | Минимальная ширина карточки поднята с 260 до 330 px |
| V14 | панель «Начало работы» | Строка «Статусы проставлены **(статус)**» | Скобка убрана |
| V15 | `/dashboard` админа | Подпись «Доступно только директору школы» на экране, который открыл администратор | «Вся школа одним взглядом: средние, готовность и пять доменов» |
| V16 | `/prep` | График динамики: все баллы одинаковые, линия прижата к нижнему краю и читается как падение в ноль | Линия по центру плюс подпись «Все N попыток с одинаковым баллом — динамики пока нет» |

## Найдено сквозным сценарием

Сценарий из девяти шагов (`e2e/tests/journey.spec.ts`) — тот самый «главный
результат фазы»: он ловит не сломанные функции, а места, где непонятно,
что делать дальше.

| № | Шаг | Заминка | Как закрыли |
| --- | --- | --- | --- |
| J1 | 2 — администратор заводит ученика | «Таблица» у администратора отвечала «У вашей роли нет домена для редактирования». Пункт меню вёл в тупик, а кнопка «Завести ученика» жила именно на этом экране | У администратора «Таблица» — реестр школы: кто учится, в каком классе и группе, с заведением и переходом на карточку |
| J2 | 2 → 4 — ученик входит впервые | Администратор завёл карточку и учётную запись, но связать их нечем: поля «привязать аккаунт» в интерфейсе нет и не было. Ученик входил в пустой кабинет, квиз отвечал «Квиз проходит ученик» | Карточка и учётная запись связываются по общей почте автоматически, в обе стороны (`students/linking.py`) |
| J3 | 7 — директор кладёт программу ученику | `POST /api/student-universities/` создавал запись без ученика: `student` не доменное поле, и сериализатор держал его только на чтение | `perform_create` ставит ученика явно — как в дочерних таблицах учеников |

## Принятые ограничения

* Фильтры дат в истории загрузок показывают формат `mm/dd/yyyy`: это подсказка
  нативного `input[type=date]`, её формат задаёт локаль браузера, а не страница.
  Свой датапикер ради этого не пишем.

## Закрыто в фазе 20

**I10 — разбор фото и `parse_university`.** Построено на распознавании
модели: `parse_certificate`, `parse_score_screenshot`, `parse_activity`
и `parse_university` вернулись в реестр команд кнопками, список
`NOT_BUILT_YET` опустел. Без ключа модели они не работают и говорят
об этом прямо, а не показывают пустой результат. Результат каждой
операции — предложение, применяет человек (инвариант №3).

**Профиль домена в `/api/meta/domains/`.** Фронт брал `models[0]`,
и справочник видов спорта, добавленный в домен в фазе 18, увёл таблицу
директора спорта на список видов спорта вместо профиля ученика. Профиль
теперь помечен флагом `is_profile`; тест `test_domains.py` не даёт домену
остаться без ровно одного профиля.

**Булево не читалось обратно из журнала.** `to_text` писал «да», а `coerce`
такое значение не принимал: молча ломались откат импорта булевой колонки
и создание записи предложением. Разбор слов «да»/«нет» вынесен
в `core.audit.to_bool`, на него есть тест.

## Найдено в фазе 30 (не чинили — вне задания)

| № | Где | Что не так | Почему это важно |
| --- | --- | --- | --- |
| ~~P1~~ | ~~`backend/config/settings/test.py`~~ | **Закрыт в фазе 31**: `test.py` фиксирует числами всё, что сверяют проверки. Прогон одинаков с заполненным `.env`, без него и с враждебными значениями | — |
| P1 (исходный текст) | `backend/config/settings/test.py` | Настройки тестов наследуются от `dev`, а тот читает `deploy/.env`. Значения контура доходят до `pytest`, и на машине с заполненным `.env` падают четыре теста: `test_short_password_is_rejected` (`PASSWORD_MIN_LENGTH=8` против ожидаемых 10), `test_weakest_is_by_recoverable_points_not_lowest_percent` (`READINESS_TALENT_TARGET=3` против 8), `test_gap_gives_partial_percent_counted_from_the_floor` и `test_formula_is_configurable` (`MATCH_FLOOR_IELTS=4.0` и веса `MATCH_W_*`) | Тот же класс, что и «тесты ходили в интернет» из фазы 29: прогон даёт разный результат на разных машинах, и человек не понимает, сломал он что-то или нет. Лечится тем же приёмом — `test.py` должен фиксировать значения, от которых зависят проверки, а не наследовать их из окружения |
| ~~P2~~ | ~~Шаблоны задач~~ | **Закрыт в фазе 31**: экран `/task-templates` у пяти директоров | — |
| P2 (исходный текст) | Шаблоны задач (`roadmap.TaskTemplate`) | В API есть полный набор действий, экрана нет ни у одной роли — шаблоны заводятся только через админку Django | Роадмап генерируется из шаблонов, а править их директору нечем: он не знает, откуда взялись задачи у его учеников |
| P3 | `frontend/src/screens/Prep.tsx`, `Materials.tsx` | Строки статусов (`ждёт сверки`, `учтён в баллах` и подобные) собраны в словарях внутри экранов, а не приходят с сервера | Тот же расход, что был с названиями полей до фазы 17: два экрана называют одно состояние по-разному, и это замечает только человек |

## Найдено в фазе 31 (не чинили — вне задания)

| № | Где | Что не так | Почему это важно |
| --- | --- | --- | --- |
| Q1 | `students.ExamAttempt.total_score` | У поля одна шкала на четыре экзамена: в реестре стоит `0…1600`, потому что там же лежит SAT. IELTS 12.5 проходит и через карточку, и через массовый ввод | Границы из реестра — единственная защита от опечатки, и на этом поле она почти не работает. Нужна шкала, зависящая от `exam_type`, а не одна на всех |
| Q2 | `materials.MaterialReport` | Автор может отозвать свою неразобранную жалобу через API, но списка своих жалоб ни на одном экране нет — отозвать её негде | Право есть, войти в него нечем. Ровно тот класс, что чинила эта фаза; страж его не поймал, потому что `useMaterialActions` встречается на экране материалов целиком |
| Q3 | `frontend/src/screens/Materials.tsx`, `Prep.tsx` | Словари статусов (`ждёт сверки`, `учтён в баллах`, названия секций) по-прежнему живут внутри экранов, а не приходят с сервера — повтор P3 из фазы 30 | Два экрана называют одно состояние по-разному, и заметит это только человек |
