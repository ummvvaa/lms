# Реестр дефектов

Обновлён: 2026-09-04, после фазы 54. Здесь только **незакрытое**. История
закрытых пунктов живёт в git и в отчётах фаз (`docs/STATE.md`); что и в какой
фазе закрыто — короткой таблицей в конце. Нумерация сквозная (`D1`, `D2`…)
и больше не повторяется; прежние обозначения из отчётов фаз сохранены
в отдельной колонке, чтобы пункт можно было найти в старых отчётах.

Каждый пункт сверен с исходниками 2026-08-27, а не переписан из прошлого
реестра. Чем проверялся — в колонке «Что не так».

Группы — по тому, кому и чем мешает, а не по фазам, в которых нашли.

## Задевает живых людей — чинить до запуска

Пусто: D27 закрыт в фазе 53, D1–D3 — в фазе 36 (см. таблицу «что закрыто»).
Новая находка этой тяжести встаёт сюда и чинится до развёртывания.

## Портит данные

| № | Прежнее | Где | Что не так | Почему это важно |
| --- | --- | --- | --- | --- |
| D4 | Q1 (фаза 31) | `students.ExamAttempt.total_score`, реестр доменов (`core/domains.py`, спецификация поля) | У поля одна шкала на четыре экзамена: `minimum=0, maximum=1600`, потому что в той же колонке лежит SAT. IELTS 12.5 проходит и через карточку, и через массовый ввод результатов. Проверено: спецификация поля в реестре не изменилась | Границы из реестра — единственная защита от опечатки, и на этом поле она почти не работает. Нужна шкала, зависящая от `exam_type`, а не одна на всех |
| D17 | — (фаза 39) | `students.ExamGoal.target_score` — тот же класс, что D4 | У целевого балла одна шкала 0–1600 на семь экзаменов, хотя у записи справочника (`ExamKind.min_score/max_score`) шкала есть: `coerce` проверяет границы по спецификации поля из реестра и границ конкретного экзамена не видит. Цель IELTS 1200 проходит. Проверено: `check_bounds` берёт только `spec_of_field` | Чинить вместе с D4 одним приёмом: границы, зависящие от значения соседнего поля (вид экзамена), должны читаться из справочника |

## Мешает работе

| № | Прежнее | Где | Что не так | Почему это важно |
| --- | --- | --- | --- | --- |
| D5 | Q2 (фаза 31) | Жалоба на материал (`materials.MaterialReport`): бэкенд `materials/views.py` («Свою жалобу можно отозвать, пока её не разобрали»), фронт `Materials.tsx`, `MaterialCard.tsx` | Автор может отозвать свою неразобранную жалобу через API, но списка своих жалоб нет ни на одном экране — отозвать её негде. Проверено: во фронте нет ни «отозвать», ни списка «мои жалобы»; директор талантов видит жалобы в очереди, автор — нет. Страж точек входа этого не ловит, потому что `useMaterialActions` встречается на экране материалов целиком | Право есть, войти в него нечем — тот самый класс дефекта, ради которого в фазе 31 поставили стража |
| D6 | V5 (фаза 35) | Дайджест (`frontend/src/screens/Digest.tsx`) | Строки последних правок показывают поле, значения и источник, но не автора. Поэтому пометка «за домен «…»» (`acting_for_title`), которую с фазы 35 отдаёт `/api/digest/`, на экране не видна. Проверено: в `Digest.tsx` нет ни одного обращения к `actor_name` или `acting_for_title` | Директор узнаёт о загрузке администратора из истории карточки или истории загрузок, но не из сводки дня — а сводка и есть то, что он открывает утром |
| D7 | R3 (фаза 32), T4 (фаза 33) | Сборка фронта (`frontend/dist`) | Один файл `index-*.js` на **1,7 МБ** (фаза 32 — 938 КБ, фаза 33 — 950 КБ, фаза 35 — 1,4 МБ; растёт с каждой фазой), кода-сплиттинга нет, Vite предупреждает об этом на каждой сборке. Проверено по размеру файла после сборки фазы 48 | Первый экран школьного контура тянет весь фронт целиком, включая экраны, которых у роли нет. На школьном интернете это секунды ожидания, и предупреждение сборки давно перестало читаться |
| D8 | — (из `docs/I18N.md`, раздел «Известные ограничения») | Переводы: шаблонные литералы фронта, `plural()/counted()`, серверные фразы (`core/phrasing.py`, дайджест, движок соответствия) | Интерфейс на английском переводится не полностью: строки с подстановками (`` `Сохранено: ${name}` ``) и короткие связки при числах (около полусотни мест) остаются по-русски; множественные формы для английского упрощены; дайджест и объяснения соответствия собираются на сервере только по-русски. Проверено: тест полноты словарей намеренно исключает шаблонные литералы; у серверных фраз словаря нет | Английский интерфейс включает человек, который по-русски не читает, — и получает смесь. Для казахского это же ограничение, но казахский из переключателя убран как черновик |
| D31 | — (решение владельца, фаза 53) | Нижний бар телефона: `frontend/src/layout/MobileNav.tsx`, `<nav className="tabbar">` | На ноутбуке бар лежит в разметке и скрыт только стилями, поэтому у страницы два лендмарка навигации вместо одного. Читалка экрана предлагает выбор из двух разделов навигации, второй из которых на этой ширине не работает. Проверено в фазе 53: именно это и ломало строгий локатор `page.locator("nav")` (D28), но сам локатор починен в прогоне, а разметка оставлена как есть | Правка на один компонент напрашивается (не рисовать бар выше 640px), но лендмарки, заголовки и порядок обхода — это одно решение по доступности на весь интерфейс, а не правка мимоходом в предделойной фазе. Владелец рассмотрит после развёртывания |

## Разработка и сборка

| № | Прежнее | Где | Что не так | Почему это важно |
| --- | --- | --- | --- | --- |
| D9 | P3 (фаза 30), Q3 (фаза 31) | `frontend/src/screens/Prep.tsx`, `components/PlatformMocks.tsx` | Подписи статусов («ждёт сверки», «учтён в баллах») зашиты в экранах, а не приходят с сервера. Проверено: строки живут в двух файлах по отдельности | Тот же расход, что был с названиями полей до фазы 17: два экрана называют одно состояние по-разному, и заметит это только человек |
| D10 | R2 (фаза 32), T3 (фаза 33) | Компоненты реестра: `popover.tsx`, `sheet.tsx` и ещё два файла в `frontend/src/components/ui/` | Длительности переходов записаны утилитами Tailwind в разметке (`duration-100`, `duration-150`, `duration-200`), а не токенами `--dur-*`. Проверено: 4 файла. Все значения в пределах потолка 320 мс | Второй источник чисел рядом с первым: «замедлить всё» — это правка в двух местах. Лечится так же, как высота полей: правилом по `data-slot` в наших стилях, которое переживёт `shadcn add` |
| D12 | V6 (фаза 35) | Воркер Celery в контуре разработки (`deploy/docker-compose.yml`, служба `celery`) | Не перечитывает код на бинд-маунте, в отличие от `runserver`: после правки задач и валидатора вставка администратора создавала пустые предложения старым кодом, пока воркер не перезапустили руками. Проверено: команда запуска — `celery -A config worker`, без автоперезагрузки | Любая фаза, трогающая фоновые задачи, обязана перезапускать `celery` перед браузерным прогоном, иначе проверка проходит на старом коде и молчит. Стоит добавить автоперезагрузку воркера в dev-compose |
| D13 | V7 (фаза 35) | Посев данных для прогона (`e2e/tests/seed.spec.ts`) | На живой базе после прошлых прогонов группа «11A» лежит в архиве, и первый же шаг посева падает на `POST /api/groups/` → 400 «Группа «11A» лежит в архиве — верните её оттуда»; остальные шаги пропускаются. Проверено: посев заводит группу по коду и архив не проверяет | Посев обещан идемпотентным, но идемпотентен только пока никто не удалял группу. Ему стоит возвращать группу из архива или заводить под другим кодом |
| D14 | V8 (фаза 35) | История загрузок после уборки прогона (`core.ImportBatch`) | У загрузок, сделанных одноразовой записью `*@probe.local`, после `purge_probe_users` строка читается «автор не сохранён»: `ImportBatch.actor` обнуляется `SET_NULL`, а снимка имени, как у `AuditLog.actor_title`, у истории загрузок нет. Проверено: поле `actor_title` есть только у `AuditLog` | В бою одноразовых записей нет и учётные записи не удаляются, так что строки без автора появятся только в контуре разработки. Но приём из фазы 34 стоит перенести и сюда, чтобы история загрузок читалась одинаково с журналом |
| D16 | — (фазы 37 и 49) | Два механизма «ученик внёс — директор подтверждает»: онбординг-квиз (`engagement.OnboardingAnswer`, пишет в профиль сразу, очередь `OnboardingQueue` на дашборде) и предложения (`Suggestion` role=student, не пишет до решения, очередь «От учеников») | Квиз оставлен как был намеренно: фаза 37 строила новый путь, не трогая работающий. Но у директора теперь две очереди слов ученика в разных местах и с разной семантикой применения. Проверено: `onboarding.answer` по-прежнему зовёт `apply_changes` напрямую | После фаз 38–39 квиз стоит перевести на тот же `Suggestion`: одна очередь, одна семантика «до решения не в профиле», меньше кода. С фазы 49 расхождение стало виднее: в кабинете директора обе очереди стоят рядом — «Ученики заполнили о себе» и «Ждут вашего решения», — и на глаз они об одном |

| D30 | — (аудит фазы 53) | Базовые вьюхи, от которых наследуется аудитория: `materials/views.py` (`SectionViewSet` → пять маршрутов), `students/views.py` (`StudentScopedViewSet` → пять, `BaseProfileViewSet` → пять), плюс `MaterialCommentViewSet.get_queryset`, который ради видимости строит объект `MaterialViewSet(request=…, kwargs={})` и берёт его queryset | То же устройство, что у закрытого D27, только предок здесь без маршрута, и живой утечки сейчас нет — проверено чтением всех пятнадцати наследников и стражем `test_no_routed_viewset_inherits_from_another_routed_viewset`. Решение «кто это читает» принимает предок: `SectionViewSet.initial()` пускает олимпиадную группу, `StudentScopedViewSet.get_queryset` открывает ученику свои строки. Потомок его не повторяет и не отменяет — он о нём молчит. `MaterialRequestViewSet` своего `get_queryset` не имеет вовсе и показывает таблицу целиком всем, кто прошёл предка — **проверено в фазе 54 и утечкой не является** (решение владельца): `initial()` пускает ровно двоих — директора талантов и ученика из олимпиадной группы, — группа в школе одна (булев признак `Student.in_olympiad_group`, а не ссылка на группу N), учебный класс доску не делит, а в самой заявке нет ничего служебного: тема, текст, автор и статус «Открыт»/«Закрыт», без пометок директора, оценочных слов и причины отказа. Общая доска запросов внутри группы — задуманный инструмент фазы 26, а не дыра. Закреплено тестами `test_request_board_is_one_group_and_the_class_group_does_not_scope_it` и `test_request_carries_no_service_fields_for_the_student` | Наследник со строками не про ученика получит ученическую аудиторию молча — ровно так правила обзвона и попали ученику. Страж ловит только маршрут-от-маршрута; «аудитория от предка» им не ловится, её надо объявлять у каждого эндпойнта явно. Коду это не мешает сегодня, но следующий справочник в этих семьях повторит D27 |
| D18 | — (полные прогоны фаз 47 и 48) | Браузерные сценарии `prep.spec.ts` (2) и `directory.spec.ts` (2) | Четыре проверки написаны до фазы 42 и с тех пор не пересматривались: `prep` ищет кнопку «Начать» там, где с фазы 42 стоят плитки экзаменов; `directory` рассчитывает на пустой справочник, а до неё его наполняют сквозные сценарии. Проверено полным прогоном фазы 48: продукт работает, сценарии — нет. Четыре красных из `engagement` и `fixed`, попавшие сюда по ходу фазы 48, из долга убраны: они падали не из-за возраста, а потому что переделка вида унесла с главной задания на сегодня и разбивку готовности — блоки возвращены | Полный прогон перестаёт означать «всё хорошо»: красные из четырёхсот приучают смотреть мимо. Сценарии надо переписать под нынешние экраны или пометить зависимость от данных |
| D19 | — (сквозной прогон фазы 47) | Портфолио ученика: пометка предложения «ждёт проверки» и признак активности «ждёт подтверждения» (`MyData.tsx`) | Две почти одинаковые фразы означают разное: первая — что предложение ещё не решено директором, вторая — что решённая активность не помечена подтверждённой. После применения предложения строка меняет одну на другую, и человеку кажется, что ничего не произошло | Ученик не понимает, решили его вопрос или нет. Слова должны отличаться: «на проверке у директора» и «внесено, ждёт отметки» |

| D21 | — (фаза 48) | Правило по одному классу поверх компонента реестра: `.quiz__option--picked`, `.prep__option--picked` и всё того же вида | Компоненты реестра красятся правилами по двум атрибутам (`[data-slot='button'][data-variant='outline']`, вес 0,2,0), и наш класс поверх них (вес 0,1,0) молча проигрывает. Так сломался выбор варианта в квизе: нажатие проходило, а на экране не менялось ничего. В фазе 48 два места переписаны на свои элементы, но приём «класс поверх кнопки реестра» в проекте остался — проверено поиском по `className=` у `Button` | Поломка не видна ни в типах, ни в тестах, ни в консоли: она видна только глазами и только тому, кто знает, как должно быть. Стоит либо запретить класс поверх `Button` тестом, либо перевести оставшиеся места на свои элементы |

| D22 | — (полный прогон фазы 48) | Измерения движения в `phase33.spec.ts`: положение строк на 60-й миллисекунде, подложка вкладок, сетка каркаса | Под нагрузкой (полный прогон занимает три четверти часа и держит браузер, докер и сборку одновременно) выборка на 60 мс иногда попадает уже после конца перехода, и проверка падает на «строки не двигались». Проверено: те же тесты по отдельности проходят, а руками в браузере на 60 мс сетка каркаса стоит на 99px из 248→72 | Ложное красное в списке из четырёхсот проверок обесценивает весь список. Мерить надо не в фиксированной точке времени, а по нескольким кадрам подряд — или вычитать из измерения фактическое время, прошедшее до выборки |

| D23 | — (фаза 49) | Кабинеты руководителей: `core/cabinets.py`, ключ `stats` | Шесть кабинетов отдают числа одним списком `stats` с кодом, подписью и цветом, а иконку к коду подбирает фронт (`ICONS` в `screens/dashboards/cabinet.tsx`). Новое число на сервере без правки этого словаря получает иконку по умолчанию — молча и без падения. Проверено: словарь живёт отдельно от списка чисел | Тот же класс, что подписи статусов в `Prep.tsx` (D9): два источника рядом, и разъезжаются они тихо. Иконке стоит приходить с сервера вместе с числом либо проверяться тестом на полноту |

| D24 | — (фаза 50) | Цели по экзаменам: `students.ExamGoal`, ограничение `unique_active_exam_goal`, форма «Внести вручную» в `components/ExamGoals.tsx` | Вторая живая цель по тому же экзамену роняет запрос пятисоткой, а не отбивается человеческим текстом: ограничение частичное (`condition=archived_at__isnull=True`), а такие исключены из проверок DRF (`PartialUniqueMixin`, решение фазы 23) — до базы доходит `IntegrityError`. Проверено прогоном фазы 50: `POST /api/exam-goals/` → 500 «duplicate key value violates unique constraint». Тем же путём идёт и применение предложения ученика, если цель по этому экзамену уже стоит | Академический директор выбирает ученика и экзамен в форме и получает необъяснённую ошибку вместо «цель по IELTS у него уже есть». Класс общий: частичных ограничений в проекте несколько (`StudentUniversity`, задачи плана), и каждое отвечает так же |
| D25 | — (фаза 51) | Вид события в телефонной ленте: `EVENT_KIND_TITLE` в `frontend/src/components/CalendarCard.tsx` | Сервер отдаёт код вида (`exam`, `deadline`, `task`…), а слово под названием события подставляет фронт своим словарём. Тот же класс, что **D9**: подпись состояния живёт в экране, а не приходит с сервером. Проверено: в `/api/calendar/` у события есть только `kind`, человеческого названия нет | Второе место, где одно и то же состояние называется словом. Разойдётся с сервером молча — как разошлись подписи статусов подготовки |
| D26 | — (фаза 51) | Карточки таблицы на телефоне: правила по `data-label` в `components/ui.css` и своя разметка `.tblcards` в `screens/TableScreen.tsx` | Одинаковый на вид результат собирается двумя способами: обычная таблица разворачивается в карточку правилами CSS, а сетка быстрого ввода рисует карточки отдельной веткой React (там `<table>` живёт ради ходьбы по ячейкам, и разворачивать его нечем). Проверено: у карточек разные классы и разные отступы | Правка вида карточки в одном месте не доедет до другого: через фазу-другую строка таблицы директора и строка реестра будут выглядеть по-разному на одном и том же телефоне |

## Требует проверки

| № | Прежнее | Где | Что не так | Почему это важно |
| --- | --- | --- | --- | --- |
| D15 | — (из раздела «Долги» в `STATE.md`) | Извлечение дедлайнов и требований с сайтов вузов (`universities/sync.py`, `suggestions/verify_requirements.py`) | В `STATE.md` было записано, что извлечение работает только на англоязычных страницах и что вёрстка каждого вуза своя. Проверить без живого ключа модели нечем: тесты к провайдеру не ходят (решение от 2026-08-24), а `check_llm` проверяет операции, не языки страниц. Открыт или уже нет — не определить | Если ограничение живо, сверка по казахстанским и российским вузам будет молча отвечать «данных нет». Нужен один прогон `check_llm` с живым ключом по странице не на английском — и пункт либо уходит в «Мешает работе», либо закрывается |

## Что закрыто и где искать

Одной строкой на группу; подробности — в отчёте соответствующей фазы
в `docs/STATE.md` и в истории git.

| Прежние обозначения | Закрыто в фазе |
| --- | --- |
| D29 — эталоны раскладки снимали экран вместе с данными и краснели после каждого переобнуления базы. Сравнение вынесено в свой проект `baseline` с фиксированным посевом (`seed-baseline.spec.ts`), закреплёнными датами и маской на обратный отсчёт; 22 телефонные проверки фазы 51 больше не срываются хвостом. Часть D30 про `MaterialRequestViewSet` снята с обоснованием — там задуманная общая доска, а не утечка | 54 |
| D27 — правила обзвона читал любой вошедший, включая ученика: `CallRuleViewSet` наследовал аудиторию у `HomeCueViewSet`. Наследование разорвано, чтение и запись закрыты владельцем домена по реестру (`DomainOwnerPermission`); D28 — второй `<nav>` от нижнего бара ломал строгий локатор в `phase36.spec.ts` и обрывал разбор D1, локатор сужен и вынесен в `e2e/helpers/shell.ts` | 53 |
| D1 (T6) — потеря сессии после смены пароля; D2 (T7) — блокировка по адресу запирала школу, снять было нечем; D3 (T8) — обрыв связи читался как выход | 36 |
| B1–B5, I1–I9, C1–C4 (аудит фазы 7) | 8 |
| I10 — разбор фото и `parse_university` | 20 |
| V1–V16, J1–J3 (проверка глазами и сквозной сценарий фазы 16) | 16 |
| Профиль домена помечен `is_profile`; булево читается обратно из журнала (`to_bool`) | 20 |
| P1 — настройки тестов из окружения; P2 — шаблоны задач без экрана | 31 |
| R1 — кнопки и метки на `.btn`/`.chip` вместо реестра (при сверке: в разметке 0 мест, только комментарии в CSS) | 33 |
| S1–S9 (белый экран, шапка, вкладки, обход кнопок, обнуление) | 33 |
| T2 — ключи вместо подписей в ячейках с выбором; T5 — прогон без учётных записей | 34 |
| U1–U8 (полный прогон фазы 33) | 34 |
| V1–V3 фазы 35 — экраны загрузки требований и банка заданий, `noqa` в `probe.py` | 35 |
| D11 — `prettier` ругался на `directory.css`: файл переформатирован вместе со всем фронтом | 44 |
| D20 — в разделе «План поступления» нечего было завести: план создаётся сам при добавлении вуза, а кнопка «Создать план» осталась на случай, когда его нет | 48 |
| Наш собственный 503 с JSON читался клиентом как обрыв связи, и профтест без ключа модели показывал «Нет связи с сервером» вместо причины | 48 |
| Эссе создавалось только двумя типами из девяти: фронт слал код типа документа в поле, где помещались четыре значения (400 при выборе типа) | 49 |
| Портфолио: подписи наезжали друг на друга — `auto-fit` с нулевым минимумом резал столько колонок, сколько пар | 49 |
| Кнопки внесения в портфолио: у академических результатов формы не было вовсе, а «Загрузить» переключала вкладку вместо выбора файла | 49 |
| Пустая правая половина экрана у портфолио, эссе и всех шести кабинетов | 49 |
| Переделка вида унесла с главной ученика «задания на сегодня» и разбивку готовности по пяти доменам (C4) — блоки возвращены в новом виде, на них поставлен страж | 48 |
| Справочники конструктора эссе нельзя было удалить: правила в реестре не было, и `can_delete` отвечал «может: никто» (найдено прогоном API в фазе 45) | 45 |

Сведено как дубли: Q3 → P3 (теперь D9), T1 → R4 (теперь O7), T3 → R2 (D10),
T4 → R3 (D7). «Принятое ограничение» фазы 16 про формат поля даты
перенесено в решения владельца (O8).

---

# Решения владельца продукта

Это не дефекты: программист их закрыть не может. У каждого пункта — кто
решает и как система ведёт себя, пока решения нет.

| № | Вопрос | Сейчас | Кто решает |
| --- | --- | --- | --- |
| O1 | **Формулы статусов A / B / C и светофора дисциплины.** Школа не задала, из чего считать статус ученика | Статусы ставятся руками директором домена; панель «Начало работы» так и говорит: «формулы для него школа не задала». Внутренние ярлыки ученику не показываются (инвариант №7) | Владелец продукта вместе с пятью директорами |
| O2 | **Роль куратора.** В данных есть группы по 15–20 человек, у группы есть поле «куратор», но роли куратора в системе нет: войти как куратор и видеть только свою группу нельзя | «Куратор» — текстовое поле карточки группы (`StudyGroup.curator`). Ролей семь: ученик, пять директоров, администратор. Флаг «видит всю школу» есть только у директора школы | Владелец продукта: нужна ли шестая рабочая роль и что ей можно — читать группу, писать в какой-то домен, ставить задачи |
| O3 | **Шаблоны роадмапа правит любой из пяти директоров.** Единственное место, где нарушен принцип «у каждого поля один владелец» (инвариант №1): `roadmap.TaskTemplate` в `SHARED_MODELS`, право на правку и удаление — у всех пяти (`DELETE_RULES`, `SHARED_WRITERS`) | Шаблоны (и задачи с эссе) — сквозные: заводит, правит и удаляет любой директор; в журнале видно кто | Владелец продукта: оставить как есть, назначить владельца (например, директора школы) или ввести правило «правит тот, кто завёл» |
| ~~O4~~ | **Закрыто в фазе 36**: решение принято — при обрыве связи приложение показывает полосу «Нет связи с сервером, пробуем переподключиться» и переподключается само, на вход не уводит (D3) | — | — |
| O5 | **Три активные учётные записи `@bhs.kz`.** После фазы 34 активной должна была остаться одна — `mukhammed_tungyshbai`. На 2026-08-27 активны ещё `eldos_abdisabr@bhs.kz` и `testa@bhs.kz`; ни один сценарий прогона их не создаёт (те заводят только `*@probe.local`) | Обе записи не тронуты. Отключить их можно с экрана «Пользователи» (запись уходит в архив, журнал сохраняется) | Владелец: нужны ли они; `testa` по имени похожа на проверочную |
| O6 | **Фото грамот и материалы олимпиадников остались у директоров** при переносе загрузки файлов администратору (фаза 35). Распознавание фото грамоты и скриншота с баллами — у директоров спорта, талантов и академического (`parse_certificate`, `parse_score_screenshot`), загрузка материалов — у директора талантов и учеников группы | Так сделано осознанно: задание говорило о выгрузках таблиц, а фото и учебные материалы — документы, а не данные для импорта. Всё, что таблицей, — только у администратора | Владелец: если «файлы» означало любые файлы — перенести и это, тогда распознавание грамот тоже станет делом администратора |
| O7 | **Образцы интерфейса появились только к фазе 48** (прежние R4, T1). Файлов `tri-haraktera-ui.html` и `dvizhenie-panel.html`, по которым просили сверить характер в фазах 32–33, в репозитории так и нет | Владелец положил образец фазы 48, и внешний вид фазы 48 сделан по нему: устройство и характер повторены, код не копировался. Сам файл удалён в фазе 52 как перекрытый `docs/ui/maket-v2.html` (лежит в истории git). Вид фаз 32–33 остаётся сделанным по словесному описанию | Владелец: если образцы фаз 32–33 ещё нужны — положить их туда же; иначе пункт закрыт образцом фазы 48 |
| O8 | **Формат поля даты.** Фильтры дат в истории загрузок показывают `mm/dd/yyyy` — подсказку нативного `input[type=date]`, формат которой задаёт локаль браузера, а не страница | Принято в фазе 16 как ограничение: свой датапикер не пишем | Владелец может пересмотреть, если подсказка формата мешает; цена — свой компонент выбора даты во всём интерфейсе |
| ~~O9~~ | **Закрыто в фазе 36**: порог по адресу поднят до 100 за час и вынесен в настройки (`LOGIN_IP_FAILURES`), школьный адрес вписывается в доверенные сети (`LOGIN_TRUSTED_NETWORKS`), блокировку снимает администратор с экрана «Пользователи» (D2) | — | — |
