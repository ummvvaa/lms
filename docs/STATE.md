# Состояние проекта
Обновлено: 2026-08-31   Последняя фаза: 37 — ученик вносит, директор подтверждает

## Готово (фазы 1–36 — подробности в git и отчётах коммитов)
- 1–8: схема БД, права по доменам, батч-таблица, дашборды, движок соответствия,
  предложения, боевой контур, аудит браузером и закрытие блокеров
- 9–16: вход по паролю, каталог вузов и процент соответствия, онбординг-квиз,
  банк заданий и пробные, обнуление, мягкое удаление, поиск, автосохранение
- 17–24: словарь полей в реестре, справочники, олимпиадная группа и материалы,
  провайдер модели, боевые настройки, чистка, темы и языки
- 25–31: ИИ-помощник, живой провайдер с поиском по официальным сайтам, ученики
  списком и временные пароли, права «завести-править-удалить» без дыр, страж
  точек входа, соревнования и банк с экранами
- 32–33: React 19, стиль «Панель», две плотности, кнопки из реестра, границы
  ошибок, движение с измерениями
- 34: одноразовые записи `*@probe.local`, уборка при любом исходе, посев ролями
- 35: файлы грузит только администратор за выбранный домен, `acting_for`
  в журнале, таблица ввода как электронная
- 36: смена пароля без потери сессии, блокировка входа не запирает школу
  (порог 100, доверенные сети, снятие с экрана), обрыв связи — полоса, не выход

## Фаза 37 — ученик вносит, директор подтверждает
- Предложения от ученика — тот же `Suggestion` (role=student, source=student):
  только про себя, только поля с флагом `student_proposable` в реестре;
  ярлыки, статусы, посещаемость не предлагаются никогда (`can_student_propose`)
- В профиль до решения ничего не пишется; кабинет показывает значение
  с нейтральной пометкой «ждёт проверки», отклонённое — с причиной директора
- Очередь «От учеников» на экране предложений: сортировка по расхождению
  от границ шкалы реестра; подтвердить / поправить и подтвердить / отклонить
  с причиной / массово; решает только владелец домена (администратору 403)
- В журнале источник `student_proposal`: актор — директор, число назвал ученик
- «Бюджет» — новое поле `cost_priority` профиля поступления; ответ квиза
  о стоимости теперь ложится в него
- Главная ученика — лестница пяти шагов (`/api/journey/`, экран `/journey`):
  прогресс, состояния выполнен/текущий/пропущен/заперт, пятый шаг открывается
  после направления и списка вузов; после прохождения — обычный дашборд,
  пункт меню «Мой путь» остаётся

## Проверено в фазе 37
- pytest: 746 (+22 в `test_phase37.py` и `test_journey.py`);
  ruff, black, tsc, eslint, сборка, словари (+27 строк) — чисто
- `api_probe` под семью ролями: 0 дефектов; добавлены проверки фазы 37
  (свой балл 201, ярлык 400, чужой ученик 400, очередь по доменам,
  администратор 403, подтверждение и откат владельцем, лестница)
- Браузер узко (`phase37.spec.ts`, записи `*@probe.local`): 5 из 5 —
  ученик внёс IELTS и увидел «ждёт проверки» при нетронутом профиле;
  директор спорта строки не видит; академический подтвердил, в журнале
  `student_proposal`; пометка исчезла; лестница из пяти карточек с запертым
  планом. После прогона одноразовых записей ноль
