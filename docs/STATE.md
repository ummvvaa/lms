# Состояние проекта
Обновлено: 2026-08-31   Последняя фаза: 38 — портфолио

## Готово (фазы 1–37 — подробности в git по тегам phase-N)
- 1–16: схема БД и права по доменам, батч-таблица, дашборды, движок
  соответствия, предложения, вход по паролю, каталог вузов, онбординг-квиз,
  банк заданий, мягкое удаление, поиск, автосохранение
- 17–27: словарь полей в реестре, справочники, материалы олимпиадников,
  провайдер модели с поиском по официальным сайтам, боевой контур, темы и языки
- 28–31: помощник через модель, ученики списком и временные пароли, права
  «завести-править-удалить» без дыр, страж точек входа
- 32–34: React 19, стиль «Панель», кнопки из реестра, границы ошибок,
  одноразовые записи `*@probe.local` и посев прогона ролями
- 35–36: файлы грузит только администратор (`acting_for` в журнале), таблица
  как электронная; сессия при смене пароля, блокировки входа, режим «нет связи»
- 37: ученик предлагает данные о себе через `Suggestion` (флаг поля в реестре,
  только про себя), очередь «От учеников» по расхождению, «ждёт проверки»
  и причина отказа, источник `student_proposal`, лестница пяти шагов

## Фаза 38 — портфолио
- «Мои данные» → «Портфолио»: вкладки Обзор · Достижения · Спорт · Олимпиады
  · Документы; всё, что записала школа, осталось на обзоре (инвариант №7)
- Процент заполнения (`students/portfolio.py`) — «сколько рассказал», не
  Readiness; веса шести разделов в настройках (`PORTFOLIO_WEIGHTS`, env
  `PORTFOLIO_W_*` в обоих примерах и ENV.md); «следующие шаги» ведут во вкладки;
  внесённое и ждущее решения тоже считается
- Профиль поступления пополнен целью: `target_level`, `target_year` (плюс
  `cost_priority` из 37) — правится учеником через предложение
- Достижения/олимпиады/соревнования вносит ученик формой — уходит предложением
  Арману или Нурлыбеку (механика 37, `new_object_key`); файл-подтверждение
  сначала ложится в документы, в предложение идёт ссылка
- Документы (`students.StudentDocument`): типы аттестат/транскрипт/сертификат/
  рекомендация/паспорт/прочее, файл в `PRIVATE_MEDIA_ROOT` (как материалы),
  отдача только через `/api/documents/id/file/` с проверкой прав (чужому 404);
  чек-лист готовности; загружает и убирает (в архив) сам ученик — это документы
  человека, а не табличные данные фазы 35
- ЕНТ добавлен в `ExamType` — карточка «Академические результаты» показывает
  GPA · IELTS · SAT · ЕНТ крупными числами
- Экспорт CV: HTML-файл по запросу, на сервере не хранится, без внутренних
  ярлыков; в CV идут все внесённые активности — галочка «подтверждена» не прячет

## Проверено в фазе 38
- pytest: 759 (+13 в `students/tests/test_phase38.py`: процент и его отличие
  от Readiness, приватность файла (аноним/чужой 404/директор 200), только
  хозяин убирает, кривой файл 400, сотрудник не грузит 403, домены очередей,
  CV с данными и без ярлыков); ruff, black, tsc, eslint, сборка — чисто;
  словари +38 строк
- `api_probe`: 0 дефектов (+8 проверок: портфолио 200/403 и без ярлыков,
  CV 200/403, документы, сотрудник 403)
- Браузер узко (`phase38.spec.ts`): 6 из 6 — вкладки и процент; достижение
  с файлом → «ждёт проверки»; файл без входа не открывается; соревнование
  у Нурлыбека, достижение у Армана и не наоборот; после подтверждения запись
  в портфолио; аттестат ставит галочку чек-листа; CV содержит внесённое.
  Одноразовых записей после прогона ноль
