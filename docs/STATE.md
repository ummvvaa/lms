# Состояние проекта
Обновлено: 2026-08-31   Последняя фаза: 40 — подбор вузов

## Готово (фазы 1–39 — подробности в git по тегам phase-N)
- 1–16: схема БД и права по доменам, батч-таблица, дашборды, движок
  соответствия, предложения, вход по паролю, каталог, квиз, банк заданий,
  мягкое удаление, поиск, автосохранение
- 17–27: словарь полей в реестре, справочники, материалы, провайдер модели
  с поиском по официальным сайтам, боевой контур, темы и языки
- 28–34: помощник через модель, ученики списком, страж точек входа, стиль
  «Панель» на React 19, одноразовые записи `*@probe.local`, посев ролями
- 35–36: файлы грузит только администратор, таблица как электронная;
  сессии, блокировки входа, режим «нет связи»
- 37–39: ученик предлагает данные через `Suggestion` (очередь «От учеников»,
  «ждёт проверки», лестница шагов); портфолио с документами в закрытом
  хранилище и CV; цели по экзаменам, календарь, напоминания и автозадачи

## Фаза 40 — подбор вузов (VENTORME раздел 3)
- Прогон подбора (`universities.MatchRun` + `selection.py`) — датированный
  снимок: фильтр по специальности/уровню/странам, пять этапов с прогрессом,
  расчёт фоном в Celery; плашка поверх любого экрана (`SelectionBadge`),
  «Свернуть — расчёт продолжится»; второй одновременный прогон — 409
- Шапка результата: дата, условия, снимок профиля (GPA, IELTS, SAT, класс,
  год) — ученик видит исходные данные, а не только вывод
- Стратегия: три карточки (`strategy.py`) — модель на фактах движка, без
  ключа те же карточки правилами с пометкой; запретные слова («шанс» и др.)
  фильтруются кодом, не промптом
- Воронка 4 числами + раскрывающееся «Как считаются проценты и категории» —
  собирается сервером из тех же настроек, которыми считает движок
- Категории четыре (dream/reach/match/safety), границы в `MATCH_TIERS`
  (env, ENV.md); секции «Ещё сильные» и «Другие» (порядок по `world_rank` —
  новое поле вуза, ведёт директор); всё сворачивается
- Карточка вуза: два числа «соответствие сейчас» и «если закрыть разрывы»
  (целевые баллы фазы 39), живой разбор по позициям с пометкой, если профиль
  изменился с даты снимка; названия «не шанс» закреплены тестом по текстам
- Избранное (`FavoriteProgram`, экран `/favorites`) отдельно от списка
  подачи; сердечко-иконка в реестре иконок; «Что дальше» — три шага;
  история прогонов с «Смотреть результат» и перезапуском
- Кнопка «Создать план» появится в фазе 41 — кнопок без обработчика не ставим

## Проверено в фазе 40
- pytest: 783 (+12 в `test_phase40.py`: границы категорий из настроек,
  фильтр, воронка/секции/снимок, цель поднимает «если закрыть разрывы»,
  повторный прогон и история, чужой прогон 404, 409 на второй, избранное
  и права, стратегия и экран без «шанса», методология из настроек)
- ruff, black, tsc, eslint, prettier, сборка — чисто; словари +70 строк
- `api_probe`: 0 дефектов (+9: запуск, досчитался, методология, воронка,
  стратегия без «шанса», чужому 403, избранное по ролям)
- Браузер узко (`phase40.spec.ts`, 4 из 4): результат со всеми блоками,
  разбор процента в карточке, избранное отдельно от списка, повторный
  подбор с фильтром стран — оба в истории. Плашку фонового расчёта живьём
  не поймать — дев-справочник считается за секунды; код закрыт `active`-
  эндпойнтом в probe. Одноразовых записей после прогона ноль
