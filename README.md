# Платформа подготовки к поступлению

Внутренняя платформа частной школы. Пять директоров ведут свои домены,
ученик видит свой кабинет. Данные заводятся импортом и руками, база
стартует пустой.

## Запуск с нуля

Всё, что нужно свежему клону, — заполнить два файла окружения. Ничего
из того, что не лежит в репозитории, руками доносить не требуется.

```bash
git clone <адрес репозитория> lms && cd lms
cp deploy/.env.example deploy/.env    # заполнить пароли и, если нужен ИИ, ключ модели
docker compose up
```

`docker compose up` из корня поднимает весь контур: корневой compose
делает `include` на `deploy/docker-compose.yml`. Миграции накатываются
сами при старте бэкенда (`backend/entrypoint.sh`), **данных они не
заводят** — после первого запуска в базе нет ни одного ученика.
В ней есть только справочники, посеянные миграциями: виды экзаменов,
типы документов эссе, категории ресурсов, бейджи, сюжеты главной,
правила обзвона и вопросы профтеста. Это части системы, а не выдуманные
люди (инвариант №8).

Фронт — http://localhost:5173, API — http://localhost:8000/api/,
документация API — http://localhost:8000/api/docs/.

Стартовый справочник вузов (20 записей с пометкой «не подтверждено»)
заводится отдельной командой, а не сам:

```bash
docker compose exec backend python manage.py seed_universities
```

Первая учётная запись в новом контуре создаётся из терминала — пароль
за человека никто не придумывает, приходит ссылка:

```bash
docker compose exec backend python manage.py invite_user --email you@school.kz --role admin
```

Учётные записи всех ролей для разработки заводит команда
`python manage.py create_dev_users` — только при `DEBUG=1`, пароли берутся
из `deploy/.env`. Отключённые администратором записи команда не включает
обратно: для этого нужен явный ключ `--force`.

Браузерный прогон ходит не под ними, а под **одноразовыми записями**
`*@probe.local`: `e2e/run.sh` заводит их перед прогоном
(`create_probe_users`, пароль из `PROBE_PASSWORD` в `e2e/.env`) и убирает
насовсем после (`purge_probe_users`) — даже если прогон упал или был
прерван. После прогона в системе не остаётся ни одной лишней записи;
журнал правок остаётся с подписью автора.

## Проверки

```bash
docker compose exec backend pytest          # тесты
docker compose exec backend ruff check .    # линтер
docker compose exec backend black --check . # форматирование
docker compose exec frontend npm run typecheck   # типы
docker compose exec frontend npm run lint        # eslint
docker compose exec frontend npm run build       # сборка
cp e2e/.env.example e2e/.env && cd e2e && npm install
cd e2e && npm test                          # браузерные сценарии (run.sh)
cd e2e && python3 api_probe.py              # прогон API под всеми ролями
```

### Эталоны сравнения раскладки

Сравнение раскладки живёт в проекте `baseline` (`e2e/tests/baseline.spec.ts`)
и сверяется со снимками в `e2e/shots/baseline/`. Их **нет в репозитории**
и не должно быть: это десятки мегабайт PNG, которые к тому же зависят
от сглаживания шрифтов на конкретной машине. В свежем клоне этих проверок
нет с чем сравнивать, и они падают — это ожидаемо.

Снимки делаются на **фиксированном посеве** — проект `baseline-seed`
(`e2e/tests/seed-baseline.spec.ts`). Он обнуляет базу и заводит школу
числами, закреплёнными в коде: пятеро учеников с одними и теми же
баллами, один вуз, одна программа, один раунд, даты — не «сегодня минус
N», а записанные явно. Оба проекта идут **последними** в прогоне: до них
успевают отработать четыреста проверок, которые иначе переписали бы
данные под снимком (так и жил дефект D29), а после них не идёт ничего,
кому эта школа могла бы помешать.

Пересъёмка не требует полного прогона — достаточно двух проектов:

```bash
cd e2e
npm test -- --project=baseline-seed --project=baseline --update-snapshots
```

Полный прогон (`npm test`) снимает и сверяет их сам, в конце.

Порог расхождения задан долей (2%), а не нулём: сглаживание шрифтов даёт
фон само по себе. Расхождение выше порога — повод посмотреть глазами,
а не признак поломки.

Что всё-таки зависит от времени — обратный отсчёт «сколько дней осталось»,
который сервер считает от своего настоящего сегодня, — **маскируется**
при сравнении (список `MASKS` в `baseline.spec.ts`), а не лечится
поднятием порога. Сетку месяца в календаре сервер тоже строит от своего
дня, поэтому эталоны переснимаются, когда прогон идёт в другой день, —
как и раньше, до первой правки внешнего вида в фазе.

Телефонная ширина (390) эталонов не имеет: `phase51.spec.ts` сохраняет
снимки в `e2e/shots/phone/` для просмотра глазами, сравнения там нет.

## Документация

| Файл | О чём |
|---|---|
| [CLAUDE.md](CLAUDE.md) | правила проекта, инварианты и журнал решений |
| [docs/STATE.md](docs/STATE.md) | что сделано по фазам и что проверено |
| [docs/DEPLOY.md](docs/DEPLOY.md) | развёртывание на боевом сервере с нуля |
| [docs/ENV.md](docs/ENV.md) | переменные окружения: что будет, если не заполнить, и где взять значение |
| [docs/ADMIN.md](docs/ADMIN.md) | руководство администратора школы |
| [docs/RIGHTS.md](docs/RIGHTS.md) | кто что видит и правит, пересказом таблицей |
| [docs/DEFECTS.md](docs/DEFECTS.md) | открытые дефекты со сквозной нумерацией и вопросы к владельцу продукта; закрытое — в отчётах фаз |
| [docs/I18N.md](docs/I18N.md) | переводы: устройство, что не переводится, статус казахского |
| [docs/QUESTION_BANK.md](docs/QUESTION_BANK.md) | формат банка заданий для загрузки |
| [docs/VENTORME.md](docs/VENTORME.md) | разбор образца по разделам: что построено и где отступили |
| [docs/ui/maket-v2.html](docs/ui/maket-v2.html) | образец внешнего вида, согласованный с владельцем |
| [docs/archive/](docs/archive/) | отчёты и задания прошедших фаз: история решений, на неё ссылается CLAUDE.md |

## Структура

```
backend/    Django + DRF, приложения по доменам
frontend/   React + TypeScript + Vite
e2e/        Playwright и прогон API по ролям
deploy/     docker-compose, Caddy, бэкапы, deploy.sh, примеры окружения
docs/       документация, docs/archive — история прошедших фаз
```
