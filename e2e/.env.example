# Пароль одноразовых учётных записей браузерного прогона (`*@probe.local`).
# Их заводит `manage.py create_probe_users` перед прогоном и убирает
# `purge_probe_users` после; переменную команда получает отсюда.
# В репозиторий этот файл не попадает. Не короче 10 знаков.
PROBE_PASSWORD=
